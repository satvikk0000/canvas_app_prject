services:
  - type: web
    name: collaborative-drawing-app
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.x
    plan: free
